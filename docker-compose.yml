services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 3s
      timeout: 3s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules
      - /app/packages/api/node_modules
      - /app/packages/web/node_modules
      - /app/packages/admin/node_modules
      - /app/packages/ui/node_modules
      - /app/packages/crons/node_modules
    ports:
      - '3000:3000'
      - '5173:5173'
      - '5174:5174'
      - '4983:4983'
    environment:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/postgres
      BETTER_AUTH_SECRET: docker-dev-secret-minimum-32-chars-long-ok
      BETTER_AUTH_URL: http://localhost:3000
      ADMIN_SEED_EMAIL: ${ADMIN_SEED_EMAIL:-admin@curved.dev}
      ADMIN_SEED_PASSWORD: ${ADMIN_SEED_PASSWORD:-CurvedAdmin2026!}
      ADMIN_SEED_NAME: ${ADMIN_SEED_NAME:-Admin}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_REGION: ${AWS_REGION:-eu-north-1}
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
